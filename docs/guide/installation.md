# Installation

```bash
# install Gridsome globally
yarn global add gridsome-cli # OR npm install -g gridsome-cli

# create a new project
gridsome create awesome-site
cd awesome-site

# start building
gridsome develop
```
